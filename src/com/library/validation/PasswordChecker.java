package com.library.validation;

public class PasswordChecker {
	public boolean isValidPassowrd(String password) {
		/*
		 * TODO: Implement logic which will validate provided password by these
		 * criteria: checks if the password is at least X long, checks for uppercase
		 * characters, checks for a special character (list of special characters must
		 * be configured)
		 */
		throw new UnsupportedOperationException("Not implemented");
	}
}
